# Fase 09 — Meta MCP + Ads

Implementar integralmente `docs/META_MCP.md`: conexão/ativos locais, adapter Meta Marketing API, AdProfile, campaign/adset/creative/ad links, insights, UI Meta Ads e `apps/meta-mcp` usando SDK MCP oficial.

Validar documentação Meta atual, Housing/Special Ad Category, scopes e versão. Em dev criar apenas mock/dry-run/PAUSED. Testar tools por stdio. Ao final, habilitar `aluguei-meta` em `opencode.json` se o comando local estiver funcional.

## Processo

Siga `orchestration/00_MASTER.md`. Não avance até gates relevantes verdes. Atualize o estado e faça commit antes de iniciar a próxima fase.
