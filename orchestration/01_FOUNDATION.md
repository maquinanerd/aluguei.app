# Fase 01 — Foundation

Bootstrap do monorepo e infraestrutura de desenvolvimento.

Entregar: pnpm/turbo, apps e packages vazios compiláveis, TypeScript strict, Fastify API health, Next web shell, Expo mobile shell, worker shell, config tipada, PostgreSQL schema/migrations base, Redis adapter opcional, storage interface, observabilidade básica, CI, lint/typecheck/test/build scripts.

Não implementar domínio de negócio ainda. Detectar versões estáveis atuais e piná-las. Registrar ADR se desviar da arquitetura.

## Processo

Siga `orchestration/00_MASTER.md`. Não avance até gates relevantes verdes. Atualize o estado e faça commit antes de iniciar a próxima fase.
