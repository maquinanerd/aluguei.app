# Phase 06 — Inbox, WhatsApp, Visits & Proposals

Inbox shell:
conversation list + active conversation + CRM context.

Implementar:
- search/filter/unread/owner/queue;
- messages;
- attachments/templates se endpoint existir;
- handoff;
- property/lead context;
- visit list/calendar/schedule/detail/reschedule/cancel;
- proposals lifecycle and detail.

Não enviar WhatsApp real em testes.

Commit: `feat(web): complete inbox visits and proposals UI`
