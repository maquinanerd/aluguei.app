# Phase 05 — Properties, Media, Listings & Channels

Implementar:
- property table;
- operational grid;
- filters/views;
- Property 360;
- owner relation;
- financial terms;
- features;
- media management;
- listings;
- channel publication;
- channel sync/errors/retry;
- property create/edit flow usando contracts reais.

Voice/AI UI apenas se backend capability atual suportar:
record/upload → transcription → structured extraction → review → media → AI suggestion → human review.

IA não converte inferência em fato.

Commit: `feat(web): complete property and distribution UI`
