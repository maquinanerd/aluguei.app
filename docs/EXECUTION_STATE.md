# Estado da execução autônoma

- current_phase: 33 (frontend do AchouImóvel — Ondas 0, 1A, 1B, 2A e 2B concluídas e no ar em `achouimovel.online`; Onda 3 a seguir), em paralelo ao Gate G4 (Fase 7.1–7.3 da auditoria 2026-09-10: Storage, Asaas e Clicksign em sandbox), que segue aguardando credenciais de sandbox do usuário
- status: **G3 PASSED** (2026-09-22). Todas as trilhas do G3 (C, D, E1, E2, F, F2 e G) mescladas e implantadas na homologação, com a aplicação `running:healthy`; relatório em `docs/audits/2026-09-10/G3_EXECUTION_REPORT.md`, decisões ADR-063 a ADR-092. O G4 depende de credenciais de sandbox (Storage, Asaas, Clicksign) que só o usuário tem
- last_completed_phase: G3 (Fases 5 e 6 da auditoria 2026-09-10) — relatório em docs/audits/2026-09-10/G3_EXECUTION_REPORT.md: regras e ciclos de vida (finanças e locação, cadastros e identidade, portal, vistoria, WhatsApp) e operação mínima (fail-fast, logs de job, OTEL, backup cifrado com restauração provada, CHECKs de domínio). Antes dele, G2 (Fases 3 e 4 da auditoria 2026-09-10) — relatório em docs/audits/2026-09-10/G2_EXECUTION_REPORT.md. Antes dele, G1 (Fases 0, 1 e 2 da auditoria 2026-09-10). Fase 0: .gitattributes (format:check no Windows), Next 16.3.0 → 16.3.4 (2 RCE críticas, P0-06), Fastify 5.12.3, overrides fast-uri, react-hooks rules-of-hooks, stack E2E descartável com boot automático, CI com serviço postgres:17 + job e2e. Fase 1: liquidação atômica (trava charge→payment + compare-and-set), provider autoritativo (webhook registra, não decide), recebimento não aplicado em conta transitória, iniciação idempotente, fila com tentativas por provider e estado terminal DEAD, migration 0012, FAKE em tabela + rota de simulação do pagador (P1-13); deadlock 40P01 achado e corrigido só na suíte de PostgreSQL real. Fase 2: ownership canônico com 404 uniforme (ADR-044) + UNIQUE(org_id,id) em 9 tabelas e 22 FKs compostas na migration 0013 (ADR-045). P0-01, P0-02, P0-03, P0-05 e P0-06 fechados com teste permanente (RED provado antes de cada fix). ADR-037..045.
- last_commit: 3f9e87a — merge do PR #35 (endereço público deixa de ser assado no build), último commit implantado, pelo deployment pxx8pes4mucrghz0sganssdw: homologação no ar em `achouimovel.online`, `app.`, `api.` e `s3.`, smoke 10/10 com canônicas e sitemap no domínio real. Antes dele, PR #34 (opção vazia derrubava a API) e PR #28 (Ondas 1A, 1B e 2A, deployment `ririajfz6wvwxk5dw76lxiyl`). Os commits seguintes em main (PRs #36 e #29) são só documentação e não foram implantados
- blockers_external: 0 de código. Dependem do usuário: credenciais de sandbox de Storage, Asaas e Clicksign para o G4; ver no painel do Coolify o log dos deploys que falharam rápido e passaram na tentativa seguinte (uyjipnqu0vlnrilunontutd4 e 5ncscvwdar3pgop0wsnlhjtk; o token não lê log); guardar uma cópia de `SERVICE_HEX_64_BACKUPKEY` fora do servidor (sem ela os backups cifrados não abrem se o VPS se perder); teste de upload de imagem na homologação (o agente não cria conta nem digita senha); revogar o token temporário do Coolify quando o trabalho pelo MCP terminar; apagar ou não o volume `aluguei-pgdata` com os dados do banco embutido (irreversível, só com pedido explícito)
- tests: passing (Onda 1A do AchouImóvel, `turbo test --force` — 1199 testes em 14 pacotes, nenhum ignorado: tests-integration 359, domain 225, integrations 181, web 181, ui 92, config 41, worker 35, api 20, storage 16, tests-contract 10, contracts 9, observability 18, meta-mcp 7, mobile 5; format:check, lint 26/26, typecheck 26/26, secret scan, audit critical, db:generate sem drift e build 12/12 verdes. `test:pg` e Playwright não rodaram nesta máquina: exigem PostgreSQL acessível — o CI os roda no PR). Antes (g3/track-g-db em 7596a0a, verificação do orquestrador, turbo test --force — 1165 testes em 14 pacotes, nenhum ignorado; test:pg 37/37; E2E Playwright 43/43). Antes (g3/track-f-ops sobre o main com as trilhas C e E1, merge 227f96e, turbo test --force — 988 testes: tests-integration 238, domain 173, integrations 172, web 156, ui 90, config 41, worker 35, api 20, observability 18, storage 16, tests-contract 10, contracts 7, meta-mcp 7, mobile 5; test:pg 19/19; E2E Playwright 35/35). Antes (g3/track-c-finance em afd59c8, turbo test --force — 837 testes: tests-integration 222, domain 171, integrations 158, web 129, ui 90, storage 16, tests-contract 10, worker 9, contracts 7, meta-mcp 7, api 5, mobile 5, config 4, observability 4; test:pg 10/10 em PostgreSQL real; E2E Playwright 32/32). Antes (g2/track-b2 em c96d6c6 — 741 testes: tests-integration 204, integrations 158, domain 120, web 105, ui 87, storage 16, tests-contract 10, worker 9, contracts 7, meta-mcp 7, api 5, mobile 5, config 4, observability 4; test:pg 9/9; E2E Playwright 28/28)
- updated_at: 2026-09-24

## Fases

- [x] 01 Foundation — monorepo pnpm+turbo, TS strict, Fastify API, Next web shell, Expo mobile shell, worker shell, packages, migration inicial, CI. ADR-004.
- [x] 02 Identity + CRM — Organization/User/Membership/RBAC, sessão opaca, audit, Party + deduplicação, Lead com funil, Task/Visit/Proposal/Timeline, UI auth/dashboard. ADR-005/006.
- [x] 03 Properties + Listings — Property/endereço privado+público/terms/owners/features/media, Listing com máquina de estado, site público, presigned upload, geocoding. ADR-007/008/009.
- [x] 04 Channel Distribution — IListingChannelAdapter + FakeChannel, fila Postgres SKIP LOCKED, estados por canal, jobs com retry/reaper, idempotência. ADR-010/011.
- [x] 05 WhatsApp + Lead Automation — WhatsAppMessenger (Meta REST + Fake), AiProvider mock, webhook inbox, gateway (vínculo/imóvel/intenção/visita/handoff), rotas conversation:* + connections. ADR-012/013/014.
- [x] 06 Inspections + AI — Inspection com máquina de estado, media com privacy guard (ADR-015), transcrição + sugestões (evidência observável), confirmação humana, comparação entrada×saída, relatório snapshot, jobs provider INSPECTION. ADR-016/017.
- [x] 07 Screening + Contracts + Signature — RentalApplication com máquina de estado, consentimento LGPD obrigatório (ADR-018), regras determinísticas explicáveis (ADR-019), screening provider (Serasa/SPC/FAKE — reais sem adapter), templates versionados + render com hash, contratos com máquina de estado, envelope de assinatura via provider + webhook inbox (ADR-020), jobs SCREENING/SIGNATURE. ADR-021.
- [x] 08 Payments + Split + Ledger — Charges com multa/juros/desconto determinísticos, pagamento via IPaymentProvider (Fake; Asaas registrado SEM adapter — ADR-024), webhook PAYMENT_CONFIRMED/REFUNDED/OVERDUE/FAILED via inbox, ledger dupla entrada (ADR-022/023), split AGENCY/LANDLORD determinístico + payout PENDING (ADR-025), banco de contas com validação de documento, reconciliação, jobs PAYMENT/PAYMENT_SCHEDULER/PAYMENT_RECONCILE. ADR-022/023/024/025.
- [x] 09 Meta MCP + Ads — Conexão Meta com token criptografado (ADR-028), ativos locais, AdProfile com validação de material/Housing/budget (ADR-031), criação de campanha CREATED_PAUSED via IMetaAdsProvider (Fake; Graph real SEM adapter — ADR-024-style), intents de alta impacto em meta_sync_jobs (ADR-029), insights em snapshots com spend em centavos, webhook via inbox (ADR-027), apps/meta-mcp com 17 tools stdio (ADR-026/030), RBAC meta:read/meta:write. ADR-026/027/028/029/030/031.
- [x] 10 Portals + Reporting — Portal externo proprietário/locatário com grant `portal_access` + token one-time → sessão opaca revogável (ADR-032), extratos (locatário/proprietário) sobre valores persistidos, contratos com content só SIGNED, vistorias como relatório estruturado sem mídia bruta (ADR-033), pagamento com QR via portal (idempotente), reporting KPIs (leads-funnel, receita mensal via ledger, spend Meta) e exportação segura CSV/JSON com RBAC report:export + rate limit + auditoria (ADR-034), paginação em payouts/reconciliations/ad-profiles, páginas web /proprietario e /inquilino. ADR-032/033/034 (+backfill 026-031).
- [x] 11 Hardening — Threat model aplicado (docs/THREAT_MODEL.md), readiness /health/ready com check de DB, rate limits sensíveis + trustProxy loopback + XFF + RedisStore (ADR-035), bodyLimit 1MB, error handler respeita 4xx de framework, secret scan local + CI (allowlist por valor), pnpm audit (informativo + gate critical), docs OPERATIONS (backup/restore) e SLO, a11y login, mobile offline banner + error boundary, testes cross-tenant ampliados (hardening 6). ADR-035.
- [x] 12 Final Audit — Auditoria ponta-a-ponta: gates completos, migrations from zero (11), E2E crítico (journey imóvel→portal), auditoria de segurança final (0 P0, 3 P1 corrigidos: webhook payments confirma no provider antes de creditar + valida ASAAS_WEBHOOK_TOKEN; WhatsApp valida X-Hub-Signature-256; UPDATE_BUDGET com caps da org na rota/tool/worker; +7 P2: ownership em intents, MCP_ALLOWED_ORG_ID, redação EAAG, refund com reversão contábil, override uuid), INTEGRATIONS.md atualizado, `docs/FINAL_REPORT.md` com classificações LIVE/SANDBOX/MOCK/IMPLEMENTED_NOT_LIVE_VERIFIED.
- [x] 13 Visual Fidelity V2 — Convergência visual com mockups Claude Design + PEG: tokens recalibrados (active nav neutro, app frame, elevação), shell contido (radius/border/bg externo), sidebar por domínio com rail colapsável + profile footer, topbar com contexto + busca global ⌘K + atendimento, dashboard operacional com dados reais (alert strip, summary cards, fila, ciclo de locação, atendimento), property list densa com contagens/tabs, novo imóvel em Focus Mode, a11y (0 P0/P1), gates green. Gap de tooling: sem Playwright/screenshots de mockup no workspace (comparação estrutural sobre markdown). Detalhes em `docs/frontend/generated/VISUAL_FIDELITY_AUDIT_V2.md` e `docs/frontend/FINAL_FRONTEND_REPORT.md` (§ Visual Fidelity V2).
- [x] 14 Visual Fidelity V3 — Refinamento final com medição real (Chrome headless + CDP): causa raiz de whitespace lateral identificada e provada — `.app-page` com `max-width: 1280px` + `margin: 0 auto` (folga 29px @1600 → 101px @1792 → 165px @1920). Workspace fluido (`--peg-page-padding: 24px`, sem max-width) em todas as telas operacionais (dashboard, listas, 360 com inspector, inbox). Fila vazia com min-height 240px (geometria estável), busca global 340px + placeholder específico, token órfão `--peg-content-max` removido. AFTER medido: page = workspace−24px/lado em 1440/1600/1792/1920; properties sem regressão; new-property Focus Mode intacto. P0 = 0, P1 = 0; gates green (lint/typecheck/test web+ui/integration 79/build/secret scan). Evidência: `docs/frontend/generated/VISUAL_FIDELITY_AUDIT_V3.md`.
- [x] 15 WhatsApp Cloud API homologation prep — Auditoria do `MetaWhatsAppAdapter` contra a doc oficial vigente (Meta migrou as docs para `/documentation/business-messaging/whatsapp/*`; fetch direto bloqueado 400 → consulta via Wayback Machine, snapshots 05/07–10/08/2026). Graph API default atualizado `v21.0 → v25.0` (changelog 2026-07-09; v20.0 expira 24/09/2026). Adicionados ao adapter (interface `WhatsAppMessenger` intacta): `sendTemplateMessage` (payload oficial `type: template` com language/components), `testConnection` (GET `/&lt;PHONE_NUMBER_ID&gt;` → metadata), `WhatsAppProviderError` tipado (`providerCode`/`providerSubcode`/`providerDetails`/`fbtraceId`/`status`/`retryable` p/ 429/5xx/throttling) e `recipient_type: individual` no envio. `whatsapp.test.ts`: 17 testes com fetch mock (template, erro 131026 tipado, rate limit retryable, timeout, payload sem id, testConnection). Doc `docs/integrations/WHATSAPP_HOMOLOGATION.md` (versão/endpoints/auth/scopes/webhook/templates/rate limits/sandbox/erros/faltas p/ homologar) + BLOCKERS/EXECUTION_STATE atualizados. Classificação: IMPLEMENTED_NOT_LIVE_VERIFIED (nenhuma chamada real).
- [x] 16 AI runtime providers — `OpenAiAiProvider` (POST /v1/chat/completions, `response_format: json_object`, `max_completion_tokens`, temperature 0) e `GeminiAiProvider` (POST /v1beta/models/{model}:generateContent, `responseMimeType: application/json`) com extração de intenção pt-BR como JSON validado por zod; `OpenAiInspectionAiProvider` (transcrição `/v1/audio/transcriptions` whisper + sugestões visuais multimodais com evidência observável). Todos com timeout AbortSignal, `AiProviderError` tipado (onError) e **fallback determinístico** para as regras existentes — nunca quebram atendimento/job. Registries `ai/` e `inspection-ai/`: `AI_PROVIDER`/`OPENAI_API_KEY`/`GEMINI_API_KEY`; default → mock (comportamento preservado; vistoria exige `fetchMedia` plugado). Docs oficiais consultadas 2026-08-17 (OpenAI Chat Completions + Audio transcriptions; Gemini discovery REST v1beta — ai.google.dev inacessível, assunção de auth `x-goog-api-key` registrada). 38 testes novos (fetch mock: sucesso, JSON inválido → fallback, timeout → fallback, auth → erro tipado/fallback, storage, registries); pacote integrations 154 verdes + tsc + eslint. Doc `docs/integrations/AI_RUNTIME.md` (estratégia por caso de uso: intent WhatsApp, transcrição, sugestões, ad copy futuro; PII/retenção/schema/revisão humana/fallback/custo/timeout; regra: IA nunca aprova crédito/assina/movimenta dinheiro/publica anúncio/altera cláusula legal). Classificação: IMPLEMENTED_NOT_LIVE_VERIFIED (nenhuma chamada real).
- [x] 17 Mobile field operation MVP — Expo (sem deps novas): `src/api.ts` com base URL de `EXPO_PUBLIC_API_BASE_URL` (default localhost:4000), captura do token de `Set-Cookie` (`aluguei_session`) e reenvio como Cookie + Bearer (ADR-005/036, token nunca logado), `ApiError` tipado (status/code/message) + falha de rede legível; router por estado no App.tsx (stack login→agenda→visit-detail→inspection→inspection-review com voltar; AppErrorBoundary + useConnectivity no topo, banner offline global); agenda com FlatList + pull-to-refresh de visitas SCHEDULED+CONFIRMED (schema visits não tem OPEN); visit-detail carrega property e inicia vistoria CHECKIN; inspection com ambientes (add room), observações (categoria/severidade/ambiente), transições DRAFT→CAPTURING→(POST process)→REVIEW→COMPLETED (409 da máquina de estado exposta na UI); inspection-review com observações + sugestões de IA; tokens PEG/brand locais; a11y (accessibilityRole/label, estados loading/empty/error/retry); `src/api.test.ts` 5 testes (sessão, erro 409, rede, merge de visitas); lint inclui src. ADR-036.
- [x] 19 Gate G1 (auditoria 2026-09-10, Fases 0–2) — **G1 PASSED**. Fase 0: `.gitattributes` corrige `format:check` em checkout Windows (P2-21), Next 16.3.4 (2 RCE críticas — P0-06), Fastify 5.12.3, `rules-of-hooks` como erro, `tests/e2e/scripts/stack.mjs` sobe cluster PostgreSQL descartável + API + worker + web sozinha (Playwright por `webServer`, porque `globalSetup` trava ao importar `.mjs` no Node 24), CI com serviço postgres:17 e job `e2e`. Fase 1 (P0-01/02/03): `settlement.ts` e `initiation.ts` novos — liquidação numa transação com trava em ordem global fixa (charge→payment) e compare-and-set, provider autoritativo (o webhook registra, nunca estorna), recebimento sem cobrança aplicável vai para `UNAPPLIED_RECEIPTS`, iniciação idempotente com 409 para tentativa em voo, ledger com business key e id determinístico, fila com tentativas por provider + estado terminal `DEAD` + claim com marca, migration 0012 (uniques parciais, CHECKs, `paid_payment_id`), FAKE com estado em tabela e rota `POST /dev/fake-payments/:id/confirm` fora de produção (P1-13). Deadlock 40P01 sob seis iniciações simultâneas: invisível no PGlite (que serializa transações), achado e corrigido na suíte `*.pg.test.ts` de PostgreSQL real. Fase 2 (P0-05): helper canônico de ownership com 404 uniforme sem oráculo de existência (ADR-044) e defesa no banco com `UNIQUE (org_id, id)` em 9 tabelas + 22 FKs compostas `(org_id, ref_id)`, `ON DELETE SET NULL` por coluna, pré-voo que aborta a migração se já houver referência cruzada (ADR-045). Evidência RED→GREEN por defeito em `docs/audits/2026-09-10/evidence/g1/`; relatório em `docs/audits/2026-09-10/G1_EXECUTION_REPORT.md`. Fora de escopo (G2): P0-04 e P0-07.
- [x] 20 Gate G2 (auditoria 2026-09-10, Fases 3 e 4) — **G2 PASSED**. Trilha A: P0-04 (versões imutáveis de contrato, gatilhos no banco), P1-06 (decisão de crédito com análise, motivo e origem), P2-08 (contrato em R$, `signature_events`), P1-11 parte interna (PDF via `pdf-lib`, provider real no envelope) e P1-05 (status da sugestão de IA); migrations 0014–0017 com pré-voo; ADR-047..051. Track B1: P0-07 (`MoneyInput` pt-BR), P1-01 (`/dashboard/summary`, `ids`/`q`, `AsyncCombobox`), P1-02 (BFF transparente), P1-03 no painel (logout confirmado pela API), P1-04, P3 (`/dev` fora de produção), interface nas regras da trilha A e foco de `Modal`/`Drawer`; ADR-052..059. Integração em `g2/integration` verificada pelo orquestrador sem cache: 9 gates com exit 0, 672 testes, `test:pg` 4/4, Playwright 18/18 (`docs/audits/2026-09-10/evidence/g2/integration/`). Homologação no Coolify com banco separado e backup, MinIO e evidência versionada (ADR-046, PR #2). Track B2 (branch `g2/track-b2`): P1-16 (acesso ao portal pela locação — link de uso único com QR, situação e revogação; `/portal/entrar`), P1-03 no portal (logout revoga a sessão no servidor), P1-17 parcial (nova candidatura com autorização LGPD, primeira publicação em canal com canais disponíveis, arquivar e reativar imóvel, ações de cobrança pelo domínio), crawler sem 4xx inesperado e Playwright dos fluxos de criação; achados na implementação e corrigidos com RED próprio: concessão duplicada do portal depois de revogar (trava da pessoa, provada em PostgreSQL real), detalhe da cobrança aberto por cima dos diálogos (`DataTable`), acentuação corrompida nas mensagens da API; ADR-061. Verificação sem cache em `c96d6c6`: 9 gates com exit 0, 741 testes, `test:pg` 9/9, Playwright 28/28 (`docs/audits/2026-09-10/evidence/g2/track-b2/`). Relatório: `docs/audits/2026-09-10/G2_EXECUTION_REPORT.md`. Mesclado no PR #5 (`a31c88e`) e implantado na homologação em 2026-09-17: smoke 14/14 (`docs/audits/2026-09-10/evidence/deploy/smoke-2026-09-17-depois.txt`).
- [x] 21 Admin da plataforma (decisão do usuário de 2026-09-15; mesclado no PR #4, `db3f95f`, e implantado na homologação) — cadastro aberto nasce em análise; aprovação, recusa e suspensão com motivo; imobiliária que não opera perde painel, portal e site público (403 `ORG_NOT_ACTIVE`, 404 no site); planos com limites de usuários, imóveis e anúncios publicados, sem cobrança (ESSENCIAL, PROFISSIONAL, ILIMITADO), checados com trava da linha da imobiliária; admins pela allowlist `PLATFORM_ADMIN_EMAILS`, com a conta criada só pelo servidor (`create-platform-admin`); telas `/situacao-da-conta` e `/plataforma`; migration 0018 (as imobiliárias existentes continuam ativas no ILIMITADO); ADR-060. RED→GREEN por parte, `test:pg` 7/7, 713 testes e Playwright 19/19 (`docs/audits/2026-09-10/evidence/platform-admin/`).
- [x] 22 Saída do PostgreSQL embutido da primeira implantação (2026-09-17, PR #7, `0673a54`) — `postgres` e `db-copy` fora do compose depois de provar, de forma indireta, que a cópia do corte de 2026-09-15 terminou sem erro (o log não é acessível: o token do Coolify não tem `read:sensitive`); o volume `aluguei-pgdata` fica, montado só para leitura por `legacy-pgdata`, porque o Docker Compose descarta volume sem uso do modelo; ADR-062. Verificação do compose: 6 falhas em `a31c88e`, 11/11 depois (`docs/audits/2026-09-10/evidence/ops/`). Implantado pelo MCP do Coolify: mesmo registro do volume, nenhuma aplicação parada, smoke 14/14 (`docs/audits/2026-09-10/evidence/deploy/coolify-mcp-2026-09-17-sem-postgres-embutido.txt`).
- [x] 23 G3 trilha C — finanças e ciclo da locação (PR #10, `8409420`, implantado na homologação em 2026-09-17 pelo deployment `hvau435f11817www8noqqxsm`, smoke 15/15) — P1-07: juros de mora de 1% ao mês **pro rata die** (antes 1% ao **dia**, sem teto: 100 dias dobravam o aluguel) e multa de 2%, agora por locação com teto de 1% a.m. e 10%, sobre aluguel e encargos; vencimento no dia da locação (1 a 28), com carência até o próximo dia útil em fim de semana e feriado bancário nacional, tudo na data civil de São Paulo; `POST /charges` só aceita data civil que existe (antes "abc" virava 500). P1-08: `lease_landlords` com a participação de cada proprietário (soma exata de 100%, senão 409), repasse dividido com `splitAmong` (um repasse e um lançamento por proprietário) e portal do proprietário pela participação. P1-20: `renew`, `readjust` e `end` com trava da locação e histórico em `lease_amendments` (mudança de aluguel encadeada, reajuste dentro da vigência), encerramento que cancela cobranças de meses posteriores sem pagamento, e scheduler por status explícito (`ACTIVE`, `DELINQUENT`, `TERMINATING`) com varredura diária. Interface: encargos, coproprietários, reajustar, renovar, encerrar, histórico e aviso de cobranças devidas na locação; participação dos proprietários no imóvel (parte do P1-17); mês de referência e vencimento na nova cobrança. Achado novo corrigido: `formatDate` mostrava data civil um dia antes no fuso do Brasil. Verificação sem cache em `afd59c8`: 9 gates com exit 0, 837 testes, `test:pg` 10/10 e Playwright 32/32; RED e GREEN por defeito em `docs/audits/2026-09-10/evidence/g3/track-c/` (com RED de concorrência sem a trava da locação). Rascunhos de ADR em `docs/audits/2026-09-10/g3/ADR_DRAFTS_TRACK_C.md`.
- [x] 24 G3 trilha E1 — portal, vistoria e handoff do WhatsApp (PR #12, `aec1ca5`, implantado em 2026-09-21 pelo deployment `5sciyp2ibxej3ybjhyunjc8e`, smoke 12/12) — P2-05: extrato do locatário sem a cobrança cancelada nos totais, `total` da lista como contagem, portal só com vistoria de entrada ou saída concluída, pagamento pelo portal com multa e juros recalculados. P1-24: evidência de vistoria fechada depois de COMPLETED (409), remoção de mídia auditada, tela sem ações de evidência na vistoria concluída. P1-18 (handoff): mensagem nova não tira a conversa da equipe, `POST /conversations/:id/resume` devolve ao bot com auditoria, caixa de entrada com "Passar para a equipe" e "Devolver ao atendimento automático". Verificação sem cache em `b6b7a02`: 9 gates com exit 0, 851 testes, `test:pg` 10/10, Playwright 35/35 (`docs/audits/2026-09-10/evidence/g3/track-e/`). A posse do número do WhatsApp e o índice parcial do portal ficam para a E2, depois da migration 0020.
- [x] 25 G3 trilha F — operação e observabilidade (PR #13, `52e2582`, implantado em 2026-09-21 pelo deployment `qctghoillmwctojqeb9g9w1s`, smoke 12/12, aplicação `running:healthy`) — P1-12: fail-fast de configuração (`NODE_ENV` obrigatório; em produção a API e o worker listam o que falta e param; FAKE, mock e dry_run só com `ALLOW_FAKE_PROVIDERS=true`, definido no compose da homologação) e `.env.example` conferido com o schema. P1-14: `REDIS_URL` não derruba a API. P1-15: `API_BASE_URL` http interno só com permissão explícita. P2-10: worker com log por job, health HTTP interno com healthcheck no compose e parada graciosa (entrada em `apps/worker/src/main.ts`). P2-11: spans OTEL de HTTP, fetch e pg, captura de erro com pilha, redação de CPF, CNPJ, e-mail, telefone e cookies, `audit_events.payload` com diff. Runner de migrations com advisory lock. Executada por agente e verificada pelo orquestrador sobre o main com C e E1: 9 gates com exit 0, 988 testes, `test:pg` 19/19, Playwright 35/35 (`docs/audits/2026-09-10/evidence/g3/track-f/`).
- [x] 26 G3 trilha F2 — backup e restauração (PR #15, `901bb3c`, implantado em 2026-09-22 pelo deployment `b0xwl7gcqhamadrqdwrlipxo`, smoke 12/12, `running:healthy`) — backup com `pg_dump` 17 cifrado com AES-256-GCM (`ALUGUEI-BKP1`), retenção dos 14 mais novos, agendado às 06:00 UTC pelo serviço `backup` do compose, com `status.json` e healthcheck de 26 h; restauração só em banco vazio, numa transação, recusando adulteração e chave errada antes de tocar o destino; chave `SERVICE_HEX_64_BACKUPKEY` do Coolify; volume `aluguei-backups`. Restauração provada em PostgreSQL real (`backup-restore.pg.test.ts`, todas as tabelas iguais linha a linha). Verificação sem cache em `eeaea75`: 9 gates com exit 0, 993 testes, `test:pg` 23/23, Playwright 35/35 (`docs/audits/2026-09-10/evidence/g3/track-f2/`). A primeira tentativa de deploy falhou sem log acessível; o job `image` do CI (PR #16) provou o build e o deploy seguinte passou (`docs/audits/2026-09-10/evidence/deploy/coolify-mcp-2026-09-22-trilha-f2.txt`).
- [x] 27 G3 trilha D — cadastros e identidade (PR #18, `0d9e352`, implantado em 2026-09-22 pelo deployment `aa0kjmizcc16e05o9oqhxark`, smoke 21/21, `running:healthy`) — P2-01: CPF e CNPJ com dígito verificador, detalhe, edição, arquivamento e documentos da pessoa. P2-02: ciclos da visita (confirmar, reagendar, cancelar com motivo) e da proposta (validade em data civil, envio, recusa, expiração diária no worker). P2-03: detalhe e edição do lead com responsável (`GET /me/members`). P2-04: troca de senha que encerra as outras sessões, recuperação de senha e convite por e-mail com token de uso único travado (provado em PostgreSQL real), mensagens só na caixa de saída local `email_outbox`. Camadas do design system corrigidas (drawer < modal < toast). Migration 0020. Na integração com o main, o Playwright passou de 10 cadastros por minuto do mesmo IP (429): o limite ficou, o proxy do painel passou a repassar o `retry-after` e os auxiliares de cadastro esperam por ele. Verificação do orquestrador em `ec3a348`: 9 gates com exit 0, 1059 testes, `test:pg` 25/25, Playwright 42/42 (`docs/audits/2026-09-10/evidence/g3/track-d/`).
- [x] 28 G3 trilha E2 — posse do número do WhatsApp e índice parcial do portal (PR #20, `bef9660`, implantado em 2026-09-22 pelo deployment `xaiauydwuqfz4g47tk06w74q`, smoke 24/24, `running:healthy`) — P1-18 (segunda parte): conexão com o token da própria conta do WhatsApp Business, cifrado (ADR-028), nasce PENDING com prazo de 24 h; o webhook só entrega a conexão VERIFIED; `POST /whatsapp/connections/:id/verify` confere o número com o token da conexão (FAKE em dry_run, sem chamada à Meta); número VERIFIED ou PENDING no prazo de outra organização → 409, reivindicação vencida só com prova na mesma requisição; auditoria de cada passo; tela com "Conectar número", "Verificar posse" e "Trocar token". Pendência do ADR-061: índice parcial `WHERE revoked_at IS NULL` com pré-voo. Migration 0021 (conexões antigas viram PENDING vencidas). Verificação do orquestrador em `51f751e`: 9 gates com exit 0, 1092 testes, `test:pg` 32/32, Playwright 43/43 duas vezes (`docs/audits/2026-09-10/evidence/g3/track-e2/`). Falha intermitente de um teste da trilha C (linha da cobrança na lista) vista uma vez em quatro rodadas completas, sem causa provada; a asserção passou a trazer o diagnóstico na mensagem. Em aberto: envio ainda pela credencial da plataforma; revalidar o token e desativar a conexão pela tela.
- [x] 29 G3 trilha G — banco (PR #22, `761f2a3`, implantado em 2026-09-22 pelo deployment `nt6zqwe3y8akzh7iquayiokm`, smoke 24/24, `running:healthy`) — P2-12: 52 CHECKs `<tabela>_<coluna>_valid` nas colunas de domínio fechado, com as listas tiradas das constantes de `packages/domain` e `packages/contracts` e um teste que compara os 65 CHECKs de vocabulário do banco com elas; `bigint` em `reconciliations.provider_total_cents` e `local_total_cents` (a conciliação quebrava acima de R$ 21 milhões); `party_consents_active_unique` declarado no schema, com teste de que índices, UNIQUEs e CHECKs do banco batem com o drizzle. Migration 0022 com pré-voo que aborta diante de dado fora do domínio; aplicada na homologação sem abortar. Verificação do orquestrador em `7596a0a`: 9 gates com exit 0, 1165 testes, `test:pg` 37/37, Playwright 43/43 (`docs/audits/2026-09-10/evidence/g3/track-g/`).
- [x] 30 Gate G3 (auditoria 2026-09-10, Fases 5 e 6) — **G3 PASSED**. Relatório `docs/audits/2026-09-10/G3_EXECUTION_REPORT.md`; ADR-063 a ADR-092 consolidados em `docs/DECISIONS.md` a partir dos rascunhos de cada trilha.
- [x] 31 Frontend do AchouImóvel — Onda 0 (diagnóstico) e Onda 1A (módulos e limites do plano). Onda 0: pacote de design em `design-source/achouimovel/` posicionado no caminho que o README e o prompt esperam, e `docs/frontend/ACHOUIMOVEL_PLAN.md` com cada tela do HANDOFF mapeada para rota atual, componentes de `packages/ui`, endpoints disponíveis e lacuna de backend — o portal não existe (`apps/portal` ausente; hoje só `/imoveis` em `apps/web`), a API pública é escopada por imobiliária e sem foto, e Vendas não tem nada no backend. Decisões do usuário delegadas e registradas em ADR-097 (portais parceiros com estado real, `PORTAL_SPEC.md` no lugar do `PROMPT_apps-portal.md` ausente, Vendas como fase própria depois da Onda 4, rotas legadas com 301 na Onda 2B). Onda 1A: `PLAN_MODULES` no domínio (CRM, ATENDIMENTO, LOCACAO, FINANCEIRO, VENDAS, MARKETING) com CHECK espelhado no banco e conferido pelo teste de vocabulário da trilha G; `plans` com `modules`, `monthly_price_cents` (só exibição, nulo = "Fale com a gente", teto do ADR-094) e `max_active_leases`; `/auth/me` devolve o plano com módulos e limites; `registerBehindModule` põe cada grupo de rotas atrás do módulo e responde 403 com `details.reason = PLAN_MODULE_NOT_INCLUDED`; limite de locações em vigor checado na transação que cria a locação (409 `PLAN_LIMIT_REACHED` antes de escrever). Migration 0024 dá a todo plano existente os cinco módulos de hoje (ninguém perde acesso) e deixa VENDAS só no ILIMITADO. ADR-095, ADR-096 e ADR-097.
- [x] 32 Frontend do AchouImóvel — Onda 1B (portal e rebrand) e Onda 2A (backend do portal, com SEO). Onda 1B: `apps/portal` (Next 16.3.4, React 19.2.3 — nenhuma versão nova), tokens da entrega, Guton por `next/font/local`, 14 componentes base e catálogo em `/dev/componentes` fora do ar em produção; na gestão, `BRAND` num arquivo só com `<title>` por template de layout (teste varre `apps/web/src` e falha se o nome antigo voltar à mão), tokens `--aluguei-brand*` → `--brand*` com alias temporário, grupo Vendas no menu, cadeado por módulo lendo `plan.modules`, tela `/app/plano` e `AvisoModoTeste`. ADR-098. Diagnóstico de SEO em `docs/frontend/PORTAL_SEO.md` (ADR-099), que ocupa o lugar do `PROMPT_apps-portal.md` ausente: padrão de URL (caminho indexa, query string não), indexação calculada pela contagem real (≥3 indexa, ≥5 estatística, ≥5 com modificador), conteúdo único por dado e não por texto gerado, título estável, JSON-LD, 410 no anúncio removido, 301 no slug trocado e lançamento por cidade em lotes. Onda 2A: finalidade e preço de venda no imóvel, 7 tipos do design, foto com legenda/ordem/capa, slug público único no país com histórico, cidade e bairro em slug, `GET /public/search` (contagem, estatística com limiar, vizinhos, `indexable`/`robots`), `GET /public/listings/:slug` (200/301/410/404 com imóveis parecidos), `GET /public/media/:id` (302 sem expor `storage_key`), `POST /public/listings/:slug/leads` (consentimento e limite por IP), alerta de busca confirmado por link de uso único e `GET /public/sitemap`. Migrations 0025, 0026 e 0027 com pré-voo e backfill.
- [x] 33 Frontend do AchouImóvel — Onda 2B (telas do portal) e domínio próprio `achouimovel.online`. Onda 2B: páginas do portal (home, `/alugar` e `/comprar` com recorte por cidade e bairro, `/imovel/[slug]`, `/imobiliaria/[slug]`, alerta de busca, mapa do site), `proxy.ts` para 410 e 301 que a página do App Router não consegue devolver, serviço `portal` no compose e alvo `portal` no Dockerfile (PR #28, deployment `ririajfz6wvwxk5dw76lxiyl`, smoke 10/10). Domínio próprio em 2026-09-24: zona no Cloudflare (plano Free) com `@`, `api`, `app`, `s3` em 62.171.164.224 e `www`, todos em **Somente DNS** — proxy ligado quebraria o desafio HTTP-01 do Let's Encrypt no Traefik; nameservers trocados na Hostinger para `meera`/`rick.ns.cloudflare.com`; no Coolify, domínio novo **somado** ao `sslip.io` de cada serviço (portal `achouimovel.online` + `www`, web `app.`, api `api.`, minio `s3.`) e `PORTAL_BASE_URL`, `APP_BASE_URL` e `API_BASE_URL` apontando para eles (`CORS_ORIGINS` é `${APP_BASE_URL}` e acompanha). Dois defeitos achados e fechados no caminho: (1) `API_BASE_URL_ALLOW_HTTP` vazio derrubava a **API** em laço de reinício — o Coolify repassa as variáveis do compose a todos os serviços e o `z.enum` recusava string vazia; como web e portal dependem de `api: service_healthy`, a homologação ficou 503 por horas (PR #34: valor vazio vale como ausente em toda opção do `envSchema`, com teste de regressão); (2) o portal servia `http://localhost:3100` na canônica, no `robots.txt` e no sitemap, porque as rotas sem parâmetro eram geradas no `next build`, onde `PORTAL_BASE_URL` não existe (PR #35: as quatro rotas passam a render por requisição, e a CI sobe um contêiner do portal com o endereço definido e exige que ele apareça no `robots.txt`). Smoke pós-implantação (deployment `pxx8pes4mucrghz0sganssdw`): `api.achouimovel.online/health`, `/health/ready`, `/public/search`, `/public/sitemap`, `app.achouimovel.online/login`, `achouimovel.online/`, `/mapa-do-site`, `/alugar/goiania-go`, `/sitemap.xml` e `www.achouimovel.online/` todos 200, canônicas e sitemap com o domínio real
